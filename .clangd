# Point clangd at the compilation database PlatformIO generates, so the editor
# resolves ESP-IDF and LVGL headers instead of flagging every include as
# missing. Regenerated by any `pio run`.
CompileFlags:
  CompilationDatabase: .pio/build/esp32-c6-lcd-1_47
